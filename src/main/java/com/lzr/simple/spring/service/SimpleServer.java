package com.lzr.simple.spring.service;

import com.lzr.simple.spring.framework.annotation.CustomService;

// just to demonstrate whether we can get value correctly
@CustomService(value = "SimpleServer")
public class SimpleServer {
}
